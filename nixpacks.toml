[providers]
bun = "1.x"

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["cd backend && bun install"]

[phases.build]
cmds = ["echo 'No build required'"]

[start]
cmd = "cd backend && bun run src/server.ts"
